/** Time (seconds) a player has to click "Ready" before being kicked */
export const READY_TIMEOUT_SECONDS = 20;

/** Time (seconds) a player has to choose an action per turn */
export const TURN_TIMEOUT_SECONDS = 15;

/** Time (ms) to wait for a server response before clearing pending action */
export const ACTION_TIMEOUT_MS = 8000;

/** Safety margin (ms) added after faint animation before hiding the sprite */
export const FAINT_ANIMATION_BUFFER_MS = 100;
